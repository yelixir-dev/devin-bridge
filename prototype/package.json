{
  "name": "devin-bridge-local-prototype",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "test": "bun test tests",
    "typecheck": "tsc --noEmit",
    "build": "bun build src/server.ts --target bun --outdir dist"
  },
  "dependencies": {
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "typescript": "^5.9.0"
  }
}
